<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="toolbarStyle">@style/SdkDemoToolBarStyle</item>
    </style>

    <style name="MobileSdkStyle" parent="Theme.MobileSdk.Light"></style>

    <style name="SdkDemoToolBarStyle" parent="Widget.MaterialComponents.Toolbar">
        <item name="android:background">@color/colorPrimaryDark</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="SdkDemoTextViewStyle" parent="Widget.AppCompat.TextView">
        <item name="fontFamily">@font/inter_regular</item>
        <item name="android:textColor">@android:color/black</item>
        <item name="android:layout_marginStart">@dimen/space_x2</item>
        <item name="android:layout_marginEnd">@dimen/space_x2</item>
    </style>

    <style name="SdkDemoButtonStyle" parent="Widget.MaterialComponents.Button">
        <item name="fontFamily">@font/inter_regular</item>
        <item name="cornerRadius">4dp</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textColor">@drawable/button_text_selector</item>
        <item name="android:layout_marginStart">@dimen/space_x2</item>
        <item name="android:layout_marginEnd">@dimen/space_x2</item>
        <item name="android:textAllCaps">false</item>
        <item name="backgroundTint">@drawable/material_button_color_states</item>
    </style>

    <style name="SdkDemoTextInputLayoutStyle" parent="Widget.MaterialComponents.TextInputLayout.OutlinedBox">
        <item name="android:layout_marginStart">@dimen/space_x2</item>
        <item name="android:layout_marginEnd">@dimen/space_x2</item>
    </style>

</resources>